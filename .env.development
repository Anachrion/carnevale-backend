# Shared, non-secret defaults for local development. Override in .env if needed.

# Base URL of the front-end app, used to build links (e.g. password reset) in emails.
FRONTEND_URL=http://localhost:56569
